# 智能销售 AI Agent - 依赖列表
# Python 3.10+

# ========== Web 框架 ==========
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# ========== 数据验证 ==========
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ========== LangChain & LangGraph ==========
langchain>=0.1.0
langchain-core>=0.1.0
langchain-community>=0.0.10
langchain-openai>=0.0.5
langgraph>=0.0.40

# ========== LLM 支持 ==========
openai>=1.10.0

# ========== 向量数据库（RAG） ==========
faiss-cpu>=1.7.4  # 或使用 faiss-gpu 如果有 GPU
tiktoken>=0.5.2

# ========== 文档生成（你的 Skills） ==========
python-docx>=1.1.0      # Word 文档
openpyxl>=3.1.2         # Excel 表格
python-pptx>=0.6.23     # PPT 演示文稿
pypdf>=4.0.0            # PDF 处理
pdfplumber>=0.10.0      # PDF 内容提取
reportlab>=4.0.0        # PDF 生成

# ========== 数据库 ==========
# PostgreSQL 持久化（LangGraph checkpointer）
psycopg[binary]>=3.1.0      # PostgreSQL 异步驱动
psycopg-pool>=3.1.0         # PostgreSQL 连接池
# redis>=5.0.0              # 会话缓存（预留）
# sqlalchemy>=2.0.0         # ORM（预留）

# ========== 工具库 ==========
aiohttp>=3.9.0
httpx>=0.26.0
python-jose[cryptography]>=3.3.0  # JWT
tenacity>=8.2.0                   # 重试机制

# ========== 开发调试 ==========
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.13.0
mypy>=1.8.0

# ========== 日志 ==========
structlog>=23.0.0  # 结构化日志（可选）
